version: '3'

services:
  cassandra:
    image: cassandra:5
    container_name: cassandra
    ports:
      - "7001:7000"
      - "9042:9042"
    environment:
      - "CASSANDRA_CLUSTER_NAME=OUR_DOCKERIZED_CASSANDRA_SINGLE_NODE_CLUSTER"