server {
  host = "0.0.0.0"
  port = 8080
  timeout = 40 seconds
}

database {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://localhost:5444/carmine"
  user = "postgres"
  password = "postgres"
}